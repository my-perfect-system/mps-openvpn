# Private keys
*.key

# Easy-RSA (external software, downloaded by download-easyrsa.sh)
common/easy-rsa/

# Client raw certs and keys (regenerated by gen-client.sh)
common/clients/

# Networks (configs, certs, keys, membership — all regeneratable)
networks/

# Environment (local overrides)
.env
